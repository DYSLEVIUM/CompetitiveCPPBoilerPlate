{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "Makefile",
			"command": "make",
			"options": {
				"cwd": "${fileDirname}"
			},
			"group": {
				"kind": "build",
				"isDefault": true
			}
		}
	]
}
